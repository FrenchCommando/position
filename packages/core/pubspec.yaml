name: position_core
description: Nostr data plane and key management for the Position app — relay client, event signing, encryption.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

# Add regular dependencies here.
dependencies:
  convert: ^3.1.2
  cryptography: ^2.9.0
  ndk: ^0.8.3
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
